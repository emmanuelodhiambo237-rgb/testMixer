backend:
  name: github
  repo: emmanuelodhiambo237-rgb/testMixer
  branch: main
  # We are switching to the "Old Reliable" bridge
  base_url: https://github-auth.netlify.app
  auth_endpoint: /auth

media_folder: "images"
public_folder: "images"

collections:
  - name: "portfolio_data"
# ... (Leave the rest of your file exactly as it is)
