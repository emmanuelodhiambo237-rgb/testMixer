backend:
  name: github
  repo: emmanuelodhiambo237-rgb/testMixer
  branch: main
  # This is the official, stable Sveltia bridge
  base_url: https://auth.sveltia.org
  auth_endpoint: /auth

media_folder: "images"
public_folder: "images"

collections:
  - name: "portfolio_data"
    label: "Portfolio Editor"
# ... (leave everything else exactly as it is)
